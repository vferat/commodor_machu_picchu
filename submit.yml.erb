batch_connect:
  template: "basic"
script:
  accounting_id: <%= auto_accounts %>
  work_dir: ~/
  native:
    ['--mem-per-cpu', '8G', '--time', '1-00:00:00', '--partition', 'debug-cpu', '--cpus-per-task', '8']


