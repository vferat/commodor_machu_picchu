batch_connect:
  template: "basic"
  work_dir: ~/
script:
  accounting_id: <%= auto_accounts %>
  native:
    ['--mem-per-cpu', '3G', '--time', '15', '--partition', 'debug-cpu', '--cpus-per-task', '1']


